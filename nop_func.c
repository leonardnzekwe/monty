#include "monty.h"

/**
 * nop_func - function to remove the top of the stack
 * @top: stack top
 * @line_number: file line number
 * Return: 0 if successful else -1
 */

int nop_func(__attribute__ ((unused)) stack_t **top
, __attribute__ ((unused)) unsigned int line_number)
{
	return (0);
}
